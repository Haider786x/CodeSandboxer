# Node.js sandbox runner image.
# node:18-alpine is intentionally pinned — do not upgrade without testing.
FROM node:18-alpine

# Use the existing non-root 'node' user provided by the base image.
USER node

WORKDIR /workspace
